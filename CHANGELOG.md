# 变更日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 计划中

- 多账号（账号池）支持 —— 目前是**单账号架构**，见 README 常见问题
- 网页面板上直接备好素材（现在要先用 `/addmat` 或填消息 ID）

## [1.0.0] - 2026-09-12

首个公开版本。核心能力：**用 MTProto 协议号（不是 Bot）把频道的消息转发到多个群，
并保留「转发自 XXX」的来源标记**。

### 新增

- **实时转发**：监听一个或多个源频道，去重后扇出到多个目标群（`forward_messages`，
  不下载重传，大文件与相册都不失真）
- **相册聚合**：同一次发出的多张图合并成一条转发，只占一次配额
- **定时重发**：把固定几条素材长期反复发到群里，素材支持区间（`6-12`）与离散 ID
- **按群配额**：每个群可以有自己的日额度，与全局总闸**各自独立**生效
- **慢速模式预判**：记住"下次可发送时刻"，发送前等待，而不是撞墙后退避
  （撞墙的代价是整个窗口白费）
- **FloodWait 自适应降速**：被限流后自动把全局速率减半、最小间隔翻倍
- **风控熔断**：`PeerFloodError` / 账号级限制触发后立刻停手并告警
- **发送节奏体检**：把「什么配法会被 Telegram 判成垃圾」写成可执行检查，
  在启动日志、`/status`、网页面板三处点名警告（见下方 1.0.0 的"修复"一节）
- **网页面板**（FastAPI + 单文件 SPA）：目标群批量管理、素材、配额、限速、
  实时日志、WebSocket 推送
- **Telegram 操控 Bot**（31 个命令）：手机上随时改配置、看状态、开关重发
- **面板与 Bot 跑在同一个进程里** —— 同一个 session 不能被两个进程持有，
  否则 `AuthKeyDuplicatedError` 会让账号被强制登出
- **运维配套**：`healthcheck.py`（15 分钟巡检 + Telegram 告警）、
  `backup.sh`（GPG 加密的每日备份）、`restore.py`（带回滚保护的恢复）、
  `login.py` / `watch_login.py`（登录流程）、`setup_commands.py`（Bot 命令菜单）
- **347 个测试**，全部不依赖网络（用假客户端）

### 修复

这些是开发过程中真实踩出来的问题，写在日志里就是为了不再踩第二次：

- **账号被限制时的失效模式**（2026-09-12 真实事故）：
  号被 Telegram 反垃圾限制后，定时重发**每 31 秒继续往被限制的号上撞了一整晚**。
  根因是定时重发直接调 `Sender.send()`，不走 `TargetWorker` 队列，
  所以"永久失败就暂停目标"那层保护对它完全无效。
  现在：单个目标报「禁止发言」→ 停发该目标 30 分钟；
  窗口内两个以上目标都报 → **判定账号级限制并熔断**；
  只配了一个群时会主动问一次 `@SpamBot` 来确认（计数法永远达不到阈值）；
  冷却期内连 API 都不碰。
- **群权限自检会误报 OK**：它读的是**群**权限，而账号被限制时群权限一切正常
  （依然是"普通成员、可发言"）。现在自检结尾会明说这个边界，
  并指向 `--probe-send` 和 `--check-account`。
- **`ChatBannedRights` 的 flag 读反了**：官方 TL 文档明确
  "the flags are inverted: if set, a flag does not allow a user to do X"，
  也就是 **True = 禁止**。旧代码把"全部为 False"（完全开放的群）判成
  "确实发不出任何东西"，方向正好搞反。顺带修正了测试夹具里同样的错误。
- **停止重发是单向操作**：`stop_repost()` 把事件永久置位、`start()` 又只在
  任务为空时创建，于是面板上点一次"停止"就只能重启进程才能恢复。
  现在 `start()` 会先清标记，并新增 `start_repost()` / `/repost on|off`。
- **`--repost-only` + 重发关闭 = systemd 无限重启**：进程直接 `return 2`，
  systemd 每 15 秒拉一次、每次都连一次 Telegram。现在改成空转待命，
  面板与 Bot 保持在线，随时可以重新开启重发。
- **永久失败白吃配额**：额度是在发送前预占的，一条根本没送到的消息不该继续占着，
  否则一个被封的目标会把当天配额整个吃光。现在永久失败会退还占位。
- **`--check` 的自检通过 ≠ 能发**：见上。总之别再只看自检结果。
- **面板日志挤成一行**：面板塞在 Python 的 raw 字符串里，
  JS 的换行必须写成 `'\n'`（一个反斜杠），写成 `'\\n'` 浏览器拿到的是字面量。
- **重发关闭时面板显示「素材 0 条」**：看起来像素材配置丢了，
  其实配置里还写着。现在退回用配置里的 ID 并标注"正文未加载"。
- **YAML 写回把 `@频道名` 写坏**：`@` 是 YAML 保留起始字符，必须加引号。
- **写回配置时注释会累积**：同一段注释被复制了 4 份，现在按"注释归属下一个键"处理。

### 安全

- 会话文件（`*.session`）等价于账号密码：`.gitignore` 已挡住，
  文档里也反复强调**绝不要让两个进程用同一个 session**
- 网页面板：令牌鉴权 + HttpOnly Cookie，默认只绑 `127.0.0.1`，
  对外暴露时走 nginx + TLS + Basic Auth
- 备份包用 GPG AES256 加密，口令单独存放

[未发布]: https://github.com/niuma1337sys/telegram-/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/niuma1337sys/telegram-/releases/tag/v1.0.0
