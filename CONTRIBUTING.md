# 贡献指南

欢迎提 Issue 和 PR。这个项目只有一个作者在维护，所以以下几点能省很多来回。

## 最基本的三条

1. **不要提交任何真实凭据**：`.env`、`*.session`、`data/`、`config.yaml` 里的真实
   频道名和群名。`.gitignore` 已经挡住了大部分，但提交前请自己 `git status` 看一眼。
   会话文件等价于账号密码。
2. **改动请带测试**。跑 `python -m pytest`，347 个用例必须全绿。
   项目里所有测试都**不依赖网络**（用 `tests/helpers.py` 里的假客户端），
   新测试也请保持这一点 —— 需要真实 Telegram 的用例没法在 CI 里跑。
3. **敏感话题请按"为什么"写注释**。这个项目里有很多看起来奇怪的代码，
   背后都是真实踩过的坑（比如 `ChatBannedRights` 的 flag 是反的、
   面板为什么要塞在 raw 字符串里）。改动这类代码时请把原因一起更新，
   否则下一个人（可能就是你）会把它改回去。

## 开发环境

```bash
git clone <你的 fork>
cd tg-relay

python -m venv .venv
# Windows: .venv\Scripts\activate
source .venv/bin/activate

pip install -r requirements.txt      # 运行 + 测试依赖都在里面
python setup_env.py                  # 交互式生成 .env（需要 api_id / api_hash）
python -m tgrelay --login            # 登录协议号，生成 data/relay.session
python -m tgrelay --check            # 自检：源/目标能否解析、能否发言、慢速多少
```

⚠️ **一个 session 只能被一个进程持有。** 本地跑过一次之后，
要么把 `data/relay.session` 删掉、要么别再让服务器同时跑 ——
两个进程同时用会让 auth key 作废、账号被强制登出。

只想看代码、不想碰真号的话：**跳过 `--login`**，直接跑测试就行。

## 跑测试

```bash
python -m pytest                     # 全部
python -m pytest tests/test_sender.py -q
python -m pytest -k write_forbidden  # 按关键字
```

所有测试都是离线的。如果某个测试需要真的连 Telegram，那它就不该被合进来。

## 代码风格

- 目标是 **能读懂**，不是能过 linter。没有配置 linter，也不要求格式化工具。
- 行宽大约 100 列。
- 中文注释和中文日志是这个项目的既定风格（用户是中文使用者），请保持一致。
- 日志用 `%s` 惰性格式化，不要在 `log.info()` 里拼 f-string。
- 新增模块请在文件头加 AGPL 许可证头（照抄 `tgrelay/alerts.py` 的开头）。

## 提交信息

一段话讲清"为什么"，而不是"改了什么" —— 改动本身 `git diff` 就能看到。
如果修的是一个具体的、能复现的问题，把现象和根因写进去。

参考现有提交：`修复账号被限制时的失效模式：自动刹车、可逆停止、自检不再误报`。

## PR

- 一个 PR 只做一件事。顺带重构请单独提。
- PR 描述里写：**现象 → 根因 → 改法 → 怎么验证的**。
- 涉及"发送行为"的改动（限速、配额、重试、风控）请特别说明影响，
  这类改动一旦出错代价是**账号被封**，评审会看得比较细。
- 新增依赖请在 PR 里说明理由；能不加就不加。

## 想加新功能？先开 Issue

尤其是这几类，动手前请先讨论，很可能被否：

| 想法 | 为什么可能不做 |
| --- | --- |
| 多账号 / 账号池 | 需要重做配额与限流模型，约 1~2 天，且要重新想清楚"哪个号发哪个群"。群数 ≤ 10 时单账号够用 |
| 去转发头（看起来像原创） | 要改成下载重传，耗带宽、大文件慢、投票/位置等特殊消息会失真，而且**更容易被判成垃圾** |
| 提频到"别人家那个速度" | 反垃圾看的是"同一个群反复出现同样的内容"。仓库里的 `tgrelay/risk.py` 就是为此写的，它能算给你看 |
| 托管成多租户服务 | 架构不支持（单进程单 session），而且那会落到 AGPL 第 13 条的网络服务义务上 |

## 许可证

提 PR 即表示你同意以 **AGPL-3.0** 授权你的贡献。
