# 运行必需
telethon>=1.36,<2

# 可选但推荐：装了就用真正的 YAML 解析；没装会退回内置的极简解析器
PyYAML>=6.0

# 走代理时必需：http/socks5 分别对应这两个
# （Telethon 不读系统代理，必须显式传 proxy，见 .env 的 TG_PROXY_*）
python-socks[asyncio]>=2.4
PySocks>=1.7

# 网页面板必需（python -m tgrelay --web）
fastapi>=0.110
uvicorn[standard]>=0.29

# 开发/测试用
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # FastAPI TestClient 依赖
