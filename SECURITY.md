# 安全策略

## 报告漏洞

**请不要开公开的 Issue。** 用 GitHub 的
[私密漏洞报告](https://github.com/niuma1337sys/tg-relay/security/advisories/new)，
或者直接联系仓库作者。

请写清楚：复现步骤、影响范围、你判断的严重程度。我会尽快回复；
这是个人项目，不承诺 SLA，但安全问题会优先看。

## 这个项目里最要紧的一条

> **`*.session` 文件等价于你的账号密码。**

Telegram 的 MTProto 用户会话（`data/relay.session`）一旦泄漏，
拿到它的人可以**直接登录你的账号**，不需要验证码、不需要两步验证
（两步验证只挡"新登录"，不挡"复用已有 auth key"）。

所以：

- 绝对不要提交 `.session` 到仓库（`.gitignore` 已挡，但请自己确认）
- 绝对不要传给任何人，包括"帮你调试"的人
- 备份里带着它，所以**备份包必须加密**（`backup.sh` 用的就是 GPG AES256）
- 怀疑泄漏了：Telegram → 设置 → 设备 → 终止该会话，然后重新 `login.py`

同理，`.env` 里的 `api_hash` 和 `TG_BOT_TOKEN` 也是凭据。

## 部署相关的注意事项

网页面板默认只绑 `127.0.0.1`。**对外暴露时请务必**：

1. 套一层 HTTPS 反向代理（nginx + Let's Encrypt）
2. 加 Basic Auth 或其它前置认证 —— 面板的令牌尽管是随机生成的，
   但它是**完整控制权**（能改配置、能触发发送、能重启重发）
3. 用 fail2ban 之类的工具挡住暴力尝试

面板令牌在 systemd 的 `Environment=` 里，注意别让它出现在任何会被公开的地方
（比如 CI 日志、Issue 里的截图）。

## 这个项目**不做**什么

- 不做"防封号"承诺。用协议号群发本身就有被限制的风险，
  仓库里的 `tgrelay/risk.py` 能帮你算风险，但算不出免死金牌。
- 不管内容合法性。使用者对自己转发的内容负责，见 README 顶部的免责声明。
- 不接收"帮我绕过 Telegram 限制"类的需求。
